function Put(X,myTitle,windowPosition,axisScale)
%function Puti(X,myTitle,windowPosition,axisScale)

Pos(windowPosition);
if nargin == 4
    imagesc(X,[axisScale]);
else
    imagesc(X);
end
axis off image;
colormap(gray);
title(myTitle);
%colorbar;